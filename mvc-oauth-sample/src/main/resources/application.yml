server:
  port: 9090

logging:
  level:
    root: info
    #'[org.springframework.web]': ERROR
    #'[br.com.k3t.mvc.oauth]': TRACE

spring:
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: demo-mvc-client
            client-secret: eQldBhLPGIRqpSRh7cUSfNRM7V9LrWqI
            scope:
              - openid
              - profile
              - email
              - address
        provider:
          keycloak:
            # A barra final é importante!
            issuer-uri: http://localhost:8080/realms/demo
